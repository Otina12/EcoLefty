@model ProblemDetails
@{
    ViewData["Title"] = Model.Title;
}

<div class="error-container">
    <h1>@Model.Status - @Model.Title</h1>
    <p>@Model.Detail</p>
    <a href="/" class="btn btn-primary">Return to Home</a>
</div>